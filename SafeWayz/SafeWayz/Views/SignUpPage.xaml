<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="SafeWayz.Views.SignUpPage">
    <ContentPage.Content>
        <ScrollView>
            <StackLayout>
         

                    <Entry  Text="{Binding Person.FullName}"   FontSize="Medium" TextColor="Black"  HorizontalTextAlignment="Center" BackgroundColor="White"/>
                    <Entry  Text="{Binding Person.FullSurname}"   FontSize="Medium" TextColor="Black"  HorizontalTextAlignment="Center" BackgroundColor="White"/>
                    <Entry  Text="{Binding Person.UserName}"   FontSize="Medium" TextColor="Black"  HorizontalTextAlignment="Center" BackgroundColor="White"/>
                    <Entry  Text="{Binding Person.Email}"    FontSize="Medium" TextColor="Black"  HorizontalTextAlignment="Center" BackgroundColor="White"/>
                    <Entry  Text="{Binding Person.Password}"    FontSize="Medium" TextColor="Black"  HorizontalTextAlignment="Center" BackgroundColor="White"/>
                    <Button Text="Sign Up" Command="{Binding SignUpCommand}"/>

      
            </StackLayout>
        </ScrollView> 
    </ContentPage.Content>
</ContentPage>